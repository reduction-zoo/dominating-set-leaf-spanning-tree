# Research campaign

This repository owns only `dominating-set-leaf-spanning-tree`. Follow `.agents/skills/research-session/SKILL.md`, `research/repository.md` and `research/reduction.md`. The fixed question is `campaigns/dominating-set-leaf-spanning-tree/question.md`. Initial budget: 20 research rounds.

Write all artifacts, comments and documentation in English. Commit the prepared independent testing foundation before constructing a candidate. Keep failed attempts and execution failures; never interpret interrupted solvers as NO-SOLUTION. Use uv with a committed lockfile. Both F and G must run in polynomial time and handle every valid target output. Include empty/boundary cases and arbitrarily large encoded integers; finite tests do not establish a general proof.

The shared experience collection is `/Users/xiweipan/Codes/autoresearch-gadgets/research/experience/entries/`; read it in place, do not copy it. You are not alone in the workspace: other campaigns run concurrently in sibling repositories. Own only this repository, preserve others' edits, and do not edit the board or another campaign. Do not publish or post upstream during this research run.

Use bounded delegation for mechanical tasks only when it saves work. If available, choose a lower-cost model for narrow lookup or formatting tasks, with explicit file ownership and concise outputs. Main researcher validates source/target semantics and oracle behavior. Keep proof decisions with the main researcher; use a fresh-context registered research-reviewer for independent review. Record actual model route and measured usage only, never estimated token savings. Avoid redundant agents, broad inherited contexts and repeated polling.

In Codex the copied `.codex/agents/research-reviewer.toml` is the reviewer registration; the DSH-only named preset is not required to continue in this authorized Codex session. Complete the campaign within its budget or record an evidence-backed stopping condition under research-session. Maintain incremental commits and current state.
